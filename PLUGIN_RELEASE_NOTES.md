# Hermes-Relay-Plugin v__VERSION__

**Release Date:** <!-- YYYY-MM-DD -->
**Since the previous plugin release:** <!-- one line: the theme of this release -->

<!-- One short paragraph: what this plugin/relay release is about and who should care. -->

<!--
═══ RELEASE-PREP CHECKLIST (delete this comment block when done) ═══
• This file is the GitHub Release body for `plugin-v*` tags. The release workflow
  substitutes __VERSION__ with the tag version, so leave that token in the Install
  section — do NOT hardcode the number there.
• Rewrite the Summary + the Added/Changed/Fixed groups below from the
  plugin/relay-relevant bullets in CHANGELOG.md's promoted version block.
• Keep-a-Changelog rules: include only the groups that have entries; delete empty ones.
• Scrub for public distribution (RELEASE.md §2): no personal names, no private infra
  (hostnames/IPs/SYSTEM.md), no fork-branch plumbing, no AI self-narration.
═══════════════════════════════════════════════════════════════════
-->

## What's changed

### Added
-

### Changed
-

### Fixed
-

## Install

```bash
pip install hermes-relay==__VERSION__
```

## Verify

```bash
python -m relay_server --help
```

---

Tag prefixes: Android releases use `android-v*`, CLI releases use `cli-v*`. Historical
relay/plugin releases used `relay-v*` tags.
