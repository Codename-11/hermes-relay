{
  "name": "hermes-relay-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "assets:sync": "node scripts/sync-assets.mjs",
    "locales:validate": "python ../scripts/check-user-docs-locales.py",
    "routes:validate": "node scripts/validate-route-contract.mjs",
    "predev": "npm run assets:sync && npm run routes:validate && npm run locales:validate",
    "dev": "vitepress dev",
    "prebuild": "npm run assets:sync && npm run routes:validate && npm run locales:validate",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "search-insights": "^2.17.3"
  },
  "dependencies": {
    "@vue-flow/core": "^1.48.2"
  }
}
